enum CharacterType {
  cosmo('Cosmo'),
  zina('Zina'),
  unknown("Unknown");

  final String name;
  const CharacterType(this.name);
}

enum GenderType {
  male('Male'),
  female('Female'),
  unknown("Unknown");

  final String name;
  const GenderType(this.name);
}
